db:
  image: mysql:8.0
  restart: always
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_DATABASE: registration-db
  volumes:
    - db_data:/var/lib/mysql
    - ./sql/user_db.sql:/docker-entrypoint-initdb.d/user_db.sql
